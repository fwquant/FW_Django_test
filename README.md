# FW_Django_test
一个 django 的helloworld 学习项目


# 安装django    :   pip install django
# 创建项目（末尾的 . 表示在当前目录创建，避免多一层嵌套）
    django-admin startproject Django_test .


# 创建子应用   
    python manage.py startapp strategies